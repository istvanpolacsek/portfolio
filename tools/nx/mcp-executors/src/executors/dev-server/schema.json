{
  "$schema": "http://json-schema.org/schema",
  "title": "Dev Server Executor",
  "description": "Launches the MCP Inspector web app against a built MCP server binary",
  "type": "object",
  "properties": {
    "serverScript": {
      "type": "string",
      "description": "Path to the compiled server entry point, relative to the workspace root (e.g. dist/apps/tunes-mcp/main.js)"
    },
    "envFile": {
      "type": "string",
      "description": "Environment file to load",
      "default": ".env.local"
    }
  },
  "required": ["serverScript"]
}
